---
- name: Enable CodeReady Builder Repository using ansible_architecture fact
  ansible.builtin.command: 'subscription-manager repos --enable=rhel-8-for-{{ ansible_architecture }}-crb-rpms'
  become: true
